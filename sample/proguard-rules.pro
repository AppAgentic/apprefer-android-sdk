# Sample app ProGuard rules.
#
# The AppRefer SDK ships its own consumer-rules.pro which is automatically
# applied to every consumer — we do NOT need to repeat those rules here.
# This file exists so `isMinifyEnabled = true` has a file to point at.
